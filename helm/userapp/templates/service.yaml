apiVersion: v1
kind: Service
metadata:
  name: userapp-service
spec:
  type: NodePort
  ports:
    - port: {{ .Values.userapp.server.port }}
      targetPort: {{ .Values.userapp.server.port }}
      protocol: TCP
  selector:
    app: ryzhkov-userapp